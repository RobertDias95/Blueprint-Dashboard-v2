// Q1: Vitest setup. Currently just imports jest-dom matchers (toBeInTheDocument,
// toHaveTextContent, etc.) so component tests in later quarters can use them.
// Add module-level mocks here as they become needed (Supabase mock, router mock).
import '@testing-library/jest-dom/vitest';
